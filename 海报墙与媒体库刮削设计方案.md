# 海报墙与媒体库刮削设计方案

> 版本：v1.2　|　状态：方案评审稿（仅设计，不改代码）　|　适用范围：局域网存储媒体播放器「海报墙 + 元数据刮削」
> 变更：v1.2（2026-08-27）新增 **§6.5 无服务端刮削的效率与准确率策略**（本地元数据缓存池 + 增量扫描 + 限流，对标网易爆米花本地化）；v1.1 从「三表完整媒体库」改为「**一张条目表 + 分组键缓存**」的中等落地强度，海报墙与文件浏览**并行共存**。

> **目标**：将首页从「文件/最近播放列表」升级为 **Emby / Jellyfin 式海报墙**，支持用户自定义媒体库源文件夹、TMDB 刮削元数据、海报墙网格展示与点击播放——**且不与现有文件浏览冲突，不引入第二套"文件-条目"权威数据源**。

本方案沿用既有 clean architecture + 多模块约定（`core:*` / `feature:*` / `player:*`），技术栈全部落在现有约束内：Kotlin + Coroutines + StateFlow、Compose + Material 3、Hilt 2.60.1、Room 2.8.4、Coil 3、Retrofit + Moshi、media3。

---

## 1. 背景与目标

### 1.1 现状一句话
现有的首页与文件浏览是**文件/目录为核心**的组织方式：播放历史的「最近播放」卡片、`video` 表（[VideoEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/VideoEntity.kt)）按单个文件扫描。缺少「**影视条目级**」的组织：没有把同一部电影的多份文件、或一个剧集的各集聚合成一张海报卡片，也没有名称清洗、元数据抓取、海报展示能力。

### 1.2 需求拆解（对齐你的目标）
| 需求 | 说明 |
|------|------|
| 媒体库源文件夹自定义 | 用户指定「某存储源里的某个子文件夹」作为一个媒体库源，并声明其内容类型（电影/剧集/其他）与刮削开关 |
| 刮削能力 | 从文件名解析标题/年份/剧集号 → 调 TMDB 抓元数据（标题、海报、背景、简介、评分、类型、演员）→ 本地缓存 |
| 海报墙显示 | Emby 式海报网格（含横幅/筛选），点击进入详情 → 解析到真实视频文件 → 走现有播放链路 |

### 1.3 设计立场（本次评审的结论）
- **海报墙 ≠ 另一套媒体库**，而是对现有文件浏览数据的**「标题级聚合视图层」**。
- **并行并存**：文件浏览原样保留；海报墙作为新增入口/视图，两套入口共享同一份文件数据与播放链路。
- **单数据源、不漂移**：不建独立的"文件↦条目"持久化映射表；文件与海报卡片的对应关系实时/缓存推导，始终与文件浏览一致。
- 刮削器一期只接 **TMDB**（免费、Kotlin SDK 成熟）；**豆瓣/中文源**作为后续可选 Provider，接口预留。
- 不建 Jellyfin/Emby **服务器**，只做**本地刮削 + 本地展示**（数据存本机 Room）。

---

## 2. 现状盘点（与本需求相关的既有能力）

- [MediaLibraryEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/MediaLibraryEntity.kt) —— `media_library` 表：存储**源根**（LOCAL/EXTERNAL/SMB/WEBDAV/OTHER/QUICK_ACCESS），唯一索引 `(url, media_type)`。用于标记海报墙条目所属存储源。
- [VideoEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/VideoEntity.kt) —— `video` 表：单个视频文件扫描结果（`filePath` 唯一索引），由 [VideoScanner.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/storage/src/main/java/com/nichx/niplayer/storage/scanner/VideoScanner.kt) 合并 MediaStore + 扩展目录生成。**文件级**，将新增 `group_key` 分组缓存列。
- [Storage.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/storage/src/main/java/com/nichx/niplayer/storage/Storage.kt) —— 统一 Local / WebDAV / SMB 的 `listFiles / openInputStream / fileExists` 等抽象；刮削扫描、图片读写、播放源解析复用它，无需关心底层协议。
- [NetworkModule.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/network/src/main/java/com/nichx/niplayer/network/NetworkModule.kt) —— 共享 `OkHttpClient` + `Moshi` + Retrofit（现有 AssrtApi / GitHubApi 模式），直接照此模式加 `TmdbApi`。
- [ThumbnailManager.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/thumbnail/src/main/java/com/nichx/niplayer/thumbnail/ThumbnailManager.kt) —— 视频缩略图生成 + 磁盘缓存 + `.thumb/` 上传，规则 `{name}-thumb.jpg`；海报图片存储可复用同一套「缓存目录 + 上传」约定。
- [NiplayerDatabase.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/NiplayerDatabase.kt) —— 主库 v15，schema 变更走 `fallbackToDestructiveMigration` 兜底（新增表用增量 Migration 或直接依赖 destructive 均可）。
- [NiNavHost.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/navigation/src/main/java/com/nichx/niplayer/navigation/NiNavHost.kt) / [Routes.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/navigation/src/main/java/com/nichx/niplayer/navigation/Routes.kt) —— 导航路由；海报墙作为新增入口需要在此登记路由。

---

## 3. 参考项目与借鉴点（调研结论）

> 来源：本轮 Web 调研。按「与你需求匹配度」归纳。

### 3.1 本地媒体库 + 刮削 + 海报墙（最贴合）
- **AI-Movie-Player**（[peixl/AI-Movie-Player](https://github.com/peixl/AI-Movie-Player)）：文件名解析→TMDB 刮削→**Poster Wall**→详情→回跳播放。链路与本需求同构，主流程可对齐。
- **LocalStream Phase 5**（[DZTic/LocalStream#37](https://github.com/DZTic/LocalStream/issues/37)）：**Retrofit 的 `TmdbApi` + Room 缓存 + Coil**、`MetadataFetcher` 编排、**TMDB 限流队列（~40 req/10s）+ 失败重试**——批量刮削必处理点，直接复用其设计。
- **Mizuu**（退役）：功能清单对照（`TMDbMovieService` / `MovieIdentification` / 网格/列表视图），仅作功能边界参考。

### 3.2 海报墙 UI（数据来自服务器，Compose 实现可直接参考）
- **JellyPlay**（[raulshma/jellyplay](https://github.com/raulshma/jellyplay)）：Compose + Material 3 Expressive，`core/` + `feature/` 多模块结构同构；海报卡片/详情页 Compose 实现是首选参考。替换其「数据源 layer」为本地聚合层即可。
- **AFinity**（[MakD/AFinity](https://github.com/MakD/AFinity)）：MVVM + Hilt + Room + Coil + **BlurHash**（海报占位/骨架屏很适合）。

### 3.3 可复用 SDK / 规范
- **tmdb-kotlin**（[ChrisKruegerDev/tmdb-kotlin](https://github.com/ChrisKruegerDev/tmdb-kotlin)）：Kotlin 版 TMDB 封装，含 `TmdbImageUrlBuilder`（poster/backdrop URL 构造），避免手写 URL。
- **Kodi NFO/图片命名规范**（[kodi.wiki Adding video sources](https://kodi.wiki/view/Adding_video_sources)）：`电影名(N年)/电影名(N年).mkv`、`<片名>.nfo`、`-poster.jpg` 等；**对齐这套命名**，用户已有 Kodi 组织好的目录可直接命中。

### 3.4 采纳结论
| 借鉴点 | 采纳方式 |
|--------|----------|
| 文件名清洗/解析 | 自研轻量 `FilenameParser`（正则：`标题 (年份)`、`标题.年份.分辨率`、剧集 `SxxExx`），对标 Kodi 规范 |
| TMDB 客户端 | `:core:network` 新增 `tmdb/*`，Retrofit + Moshi（对齐现有 AssrtApi 模式） |
| 抓取编排 + 限流队列 | `ScraperEngine`，信号量限并发 + 失败指数退避（对标 LocalStream Phase5） |
| 海报/背景图 | TMDB 图片 URL + Coil 磁盘缓存；海报墙骨架用 BlurHash |
| 海报墙 UI | 对齐 JellyPlay 的 Compose 实现思路 |

---

## 4. 目标架构

### 4.1 设计核心：分组视图层，而非第二套媒体库
```
文件浏览（现有，不动）          海报墙（新增视图）
   一台数据源                         │  复用同一份文件数据
video 表 / Storage.listFiles         ▼
   │                    LibraryGrouper
   └──────────────►  按 FilenameParser 分组成「标题级」卡片
                                │
                                ▼
                      ScraperEngine 刮削元数据 → 附着到 media_item
```
- **文件浏览链路不动**：`media_library` / `video` / 文件浏览 / 缩略图 / 备份逻辑保持原样。
- **海报墙是投影**：对"某源文件夹"的文件列表做标题聚合，产出海报卡片；不另存"文件↦条目"映射。

### 4.2 分层模型
```
UI  (feature:library → 海报墙视图 / 详情 / 源管理)
     │  StateFlow 订阅 / 触发意图
     ▼
用例层   LibraryUseCase (设为媒体库源 / 触发扫描 / 触发刮削 / 点海报组织播放)
     │
     ▼
领域层   LibraryRepository (仅 media_item 的读写 + 分组/刮削编排接口)
     │  文件↦条目关系不落库，实时/缓存推导
     ▼
数据层   Grouper + ScraperEngine（:core:library）
          ├─ LibraryGrouper：基于现有文件列表分组（复用 :core:storage 的 listFiles）
          ├─ ScraperEngine：TMDB 抓取编排（经 :core:network / :core:storage）
          └─ MediaItemDao + media_item 表（:core:database）
```

### 4.3 新增/调整模块
| 组件 | 建议位置 | 说明 |
|------|----------|------|
| `MediaItem` 领域模型 + `MediaItemDao` + `MediaItemCache` | `:core:database`（表 + Dao） | 条目表 `media_item` + 识别缓存表 `scrape_cache` |
| `video.group_key` 缓存列 | `:core:database` | 本地源分组键加速列 |
| `LibraryRepository` | `:core:library` | 读写 media_item；不持有映射表 |
| `FilenameParser` / `LibraryGrouper` / `ScraperEngine` / `MetadataCache` | `:core:library`（`scraper/` 包） | 分组 + 刮削编排 + 识别结果缓存 |
| `TmdbApi` + DTO + `Moshi` adapter | `:core:network`（新增 `tmdb/*`） | 对齐现有 `GitHubApi` 模式 |
| 海报墙 UI / 详情页 / 源管理页 | `:feature:library` | 避免继续膨胀 `:feature:home` |
| 源级开关（是否自动刮削） | `:core:datastore` | 用既有设置机制存，**不建库配置表** |

> 说明：是否新建 `:core:library` 与 `:feature:library` 由评审拍板。最小改动是先在 `feature:home` 内划清领域包；本方案按「新建模块」给出，模块内预留降级路径。

---

## 5. 数据模型设计

### 5.1 变更总览：有效表 2 张（1 条目表 + 1 识别缓存表）+ 1 个缓存列
| 变更 | 对象 | 说明 |
|------|------|------|
| 新增列 | `video` 表 | `group_key`（文件名解析出的分组键，本地索引加速） |
| **新表（条目）** | `media_item` | 一张海报卡片 = 一个影视条目的元数据 |
| **新表（缓存）** | `scrape_cache` | 识别结果缓存（`normalizedTitle, year → TMDB 数据`，TTL），实现 §6.5 本地元数据缓存池；纯缓存，非权威数据 |
| 不建 | `library_source` / `media_item_file` | 源配置并入条目列；文件↦条目不落库 |

> **要点（正是"中"与"重"的区别）**：*不建*"文件↦条目"的独立映射表，文件与海报的对应关系**实时/缓存推导**，永远与文件浏览一致、不漂移。`scrape_cache` 只是**识别结果缓存**（对应网易的云缓存本地版），不承担文件↦条目关系，不构成第二套权威数据源。

### 5.2 `media_item`（唯一的影视条目表：海报卡片）
```kotlin
@Entity(
    tableName = "media_item",
    indices = [
        // 同源同分组的条目唯一，避免重复卡片
        Index(value = ["library_id", "folder_path", "group_key"], unique = true),
        Index("content_type"),
        Index(value = ["title"], name = "index_media_item_title_sort"),
    ],
    foreignKeys = [
        ForeignKey(
            entity = MediaLibraryEntity::class,
            parentColumns = ["id"],
            childColumns = ["library_id"],
            onDelete = ForeignKey.CASCADE, // 删存储源自动清理其海报墙条目
        )
    ]
)
data class MediaItemEntity(
    @PrimaryKey(autoGenerate = true) var id: Int = 0,
    @ColumnInfo(name = "library_id") var libraryId: Int,       // FK -> media_library.id：所属存储源
    @ColumnInfo(name = "folder_path") var folderPath: String,  // 源子目录（相对存储根；根用 StorageFactory.ROOT）
    @ColumnInfo(name = "group_key") var groupKey: String,      // 分组键，与视频/列表的分组键对齐
    @ColumnInfo(name = "content_type") var contentType: ContentType, // MOVIE / TV / OTHER
    @ColumnInfo(name = "title") var title: String,             // 主标题（中文可优先）
    @ColumnInfo(name = "original_title") var originalTitle: String? = null,
    @ColumnInfo(name = "year") var year: Int? = null,
    @ColumnInfo(name = "tmdb_id") var tmdbId: Int? = null,
    @ColumnInfo(name = "imdb_id") var imdbId: String? = null,
    @ColumnInfo(name = "poster_uri") var posterUri: String? = null,     // 本地缓存路径或 TMDB URL
    @ColumnInfo(name = "backdrop_uri") var backdropUri: String? = null,
    @ColumnInfo(name = "overview") var overview: String? = null,
    @ColumnInfo(name = "rating") var rating: Double? = null,
    @ColumnInfo(name = "genres") var genres: String? = null,    // JSON 数组，防 StringList type converter 膨胀
    @ColumnInfo(name = "cast_json") var castJson: String? = null,
    @ColumnInfo(name = "scrape_state") var scrapeState: ScrapeState = ScrapeState.PENDING,
    @ColumnInfo(name = "scrape_error") var scrapeError: String? = null,
    @ColumnInfo(name = "added_at") var addedAt: Long = System.currentTimeMillis(),
    @ColumnInfo(name = "updated_at") var updatedAt: Long = System.currentTimeMillis(),
)
```
- **源配置不进独立表**：`library_id + folder_path` 已隐含"媒体库源归属"；`content_type` 直接在条目上；**「该源是否自动刮削」等源级开关放 `:core:datastore`**，以 `library_source_{libraryId}_{folderPath}` 为 key，复用现有设置机制。

### 5.3 分组键缓存列（`video.group_key`）
```kotlin
@Entity(tableName = "video", ...)
data class VideoEntity(
    // ... 现有字段 ...
    @ColumnInfo(name = "group_key", defaultValue = "")
    var groupKey: String = "", // 由 FilenameParser 解析出的标题分组键，扫描时写入
)
```
- **作用**：本地源（MediaStore / 扩展目录）海报墙分组直接查 `video.group_key` 索引列，把"实时计算"压到数据库毫秒级查询。
- **远程源（SMB/WebDAV）**：不走 `video` 表。分组键在进入该源文件夹时由 `LibraryGrouper` 基于 `listFiles` 结果计算，并以**内存态缓存**（非持久表）随源刷新——同样避免全量重扫。
- **正确性**：`group_key` 只是"派生缓存"，文件增删/改名时随扫描重建，永远最新。

### 5.4 文件↦条目 的实时解析（不落库）
点海报 → 组织播放：
```
按 media_item(libraryId, folderPath, groupKey)
   ├─ 本地源：查 video WHERE 路径属于 folderPath 且 group_key = ?
   ├─ 远程源：取该文件夹最近 listFiles 结果，按 groupKey 匹配
   ▼
得到真实 StorageFile → createPlayUrl / openPlayStream → NxMediaSource → 现有播放链
```
- 电影：同分组多文件时取主文件（按文件名/大小规则选 primary 或用户指定）。
- 剧集：同分组文件按 `SxxExx` 得季/集，详情页按集选择，点选集播放。

### 5.6 `scrape_cache`（识别结果缓存表，纯缓存非权威）
```kotlin
@Entity(tableName = "scrape_cache")
data class ScrapeCacheEntity(
    @PrimaryKey var normalizedKey: String,   // (normalizedTitle + year) 归一化 key
    @ColumnInfo(name = "json") var json: String,       // TMDB 识别结果 JSON 快照
    @ColumnInfo(name = "fetched_at") var fetchedAt: Long = System.currentTimeMillis(),
)
```
- 职责：缓存「标题 → TMDB 识别结果」，`ScraperEngine` 命中直接本地返回，零网络请求（§6.5 效率核心）。
- 与 `media_item` 解耦：缓存是"识别结果"，条目是"海报卡片"；同标题多文件/重复目录共享同一缓存。
- 维护：随 `ScraperEngine` 写入；`MetadataCache` 负责读、TTL 过期清理（如 30 天）、手动刷新覆盖。

### 5.7 迁移策略
- `video.group_key` = `ALTER TABLE ... ADD COLUMN`（增量 Migration）；`media_item` / `scrape_cache` = 新建表。均纯新增，旧数据无影响；兜底仍可用 `fallbackToDestructiveMigration`。
- 新增枚举 `ContentType`（MOVIE/TV/OTHER）与 `ScrapeState`（PENDING/SCRAPING/DONE/FAILED），仿照 [MediaType](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/enums/MediaType.kt) 以 String 存储 + Converter 映射。

> **一致性要点**：`media_item` 是"海报卡片 + 刮削元数据"的**持有层**，`scrape_cache` 是**可再生成的识别缓存**；文件分组与文件变化交给现有扫描/列表能力，条目不自带文件清单权威数据。这样刮削好了的元数据不会被扫描器冲掉，文件变化也不会让条目指向失效文件，且缓存清空后只需重新识别、无需重建条目。

---

## 6. 核心流程设计

### 6.1 媒体库源自定义（设为源 / 移除源）
1. 用户在某存储源的**文件浏览页**选中一个子文件夹 → 「设为媒体库源」。
2. 弹窗选择内容类型（电影/剧集/其他）、展示名、是否自动刮削（写入 `:core:datastore`）。
3. `LibraryGrouper` 对该文件夹做一次分组，按需生成 `media_item` 草稿（无独立源表）。
4. 立即触发一次该源的扫描/刮削。

### 6.2 分组 → 刮削
```
LibraryGrouper.group(folder, contentType)
   │ 复用 Storage.listFiles（本地 + SMB/WebDAV 通用）
   │ 逐文件 FilenameParser → (groupKey, year?, season?, episode?)
   ▼
按 groupKey 分组 → 同标题+年份文件归为一个 media_item 候选
   │  电影：同目录同标题多版本合并
   │  剧集：按标题分组，文件由 groupKey+季集 区分
   │  本地源：同时写回 video.group_key 缓存列
   ▼
写入/更新 media_item (scrape_state=PENDING)
   ▼（若该源自动刮削且网络可用）
ScraperEngine.submit(item)  ← 信号量(4) 限并发 + 失败指数退避
   │ 按 title+year 调 TmdbApi.searchMulti → 取 top 候选
   │（用户可在校验页确认，或按 年份+标题相似度 自动命中）
   ▼
抓 title/posters/backdrop/overview/rating/genres/cast
   │ poster/backdrop 下载 → 封面缓存目录（复用 thumbnail 缓存命名约定）
   ▼
update media_item (scrape_state=DONE/FAILED + 图片 uri + 元数据)
```
**批量刮削关键约束（对标 LocalStream Phase5）**：
- 并发控制：TMDB ~40 req/10s，`Semaphore(4)` + `delay` 节流。
- 增量：已 DONE 默认跳过；文件变更 → 仅更新分组，不重置已刮削元数据（除非手动「刷新元数据」）。
- 幂等：`(library_id, folder_path, group_key)` 唯一去重。

### 6.3 海报墙展示（UI）
- 首页**新增「海报墙」入口**（并行共存，不动现有英雄卡/最近播放）。`LazyVerticalGrid`（自适应列数）渲染 `media_item` 海报卡片，Coil 加载 `posterUri`，未刮削卡片显示标题占位 + BlurHash 骨架。
- 顶部：源筛选（按 `media_item` 的 library/folder 聚合）、类型筛选（电影/剧集）、排序（最近添加/评分/标题）、搜索。
- 详情：背景图 + 简介 + 评分 + 类型 + 该条目分组下的文件列表；剧集按季/集分组。
- 播放：按 `§5.4` 实时解析真实 `StorageFile` → 现有播放链。**文件浏览与海报墙点同一个文件，结果一致。**

### 6.4 网络模型与可复用性
- 存储、缩略图、播放源解析全部复用既有 `:core:storage` / `:core:thumbnail` / `player:kernel`，不重复实现协议差异。
- 图片来源统一走 Coil（[libs.versions.toml](file:///Users/nichx/Documents/GitHub/NIplayer/gradle/libs.versions.toml) 已含 `coil-network-okhttp`）+ 共享 OkHttpClient。

### 6.5 无服务端刮削的效率与准确率策略（v1.2 新增）

> **背景**：Emby/Jellyfin 有服务端可做集中批量识别；网易爆米花背靠网易云服务端 + 全球共享元数据缓存池，靠服务器池化做到"秒级首刮"。NIplayer **无服务端**，无法复刻"首刮秒回"，但可把同样思路**搬回本地**，收敛到接近的体验。核心是：**本地把结果缓存起来，二次即快**。

#### 效率策略清单
| 策略 | 做法 | 对标 |
|------|------|------|
| **本地元数据缓存池**（核心） | 刮削结果以 `(normalizedTitle, year) → TMDB 数据` 存 Room 缓存表；二次命中直接本地返回，**零网络请求**。同一标题多文件、重复目录全部秒回 | 网易云缓存 → 本地版 |
| **增量扫描** | 只处理新增/改动文件（按修改时间/大小/hash 判断），已刮削 `DONE` 的跳过，不重扫、不重置元数据 | Jellyfin 增量扫描 |
| **本地优先 / NFO 优先** | 同目录已有 `.nfo` / `poster.jpg` 时直接用，不发请求；用户用 Kodi 整理过的目录零成本命中，比 TMDB 命中更快更准 | Emby/Jellyfin 本地优先 |
| **图片落盘持久化** | 海报/背景下载后存本地缓存目录，列表渲染不再重拉图 | 复用 `:core:thumbnail` 的 `.thumb/` + 缓存命名约定 |
| **优先级队列 + 分批** | 先扫先显示（海报墙立即可用），后台继续补齐；分页/分批处理避免长期占用 | Jellyfin 优先级队列 |
| **动态限流 + 指数退避** | 按库规模调并发/间隔：小库 3–5 并发、间隔 1–2s；大库降并发、拉长到 5–10s；网络/429 失败按 `2^n` 退避 | MetaShark 动态频控 |

#### 准确率策略
- **高质量命名解析**：准确率高不在请求次数而在解析。`FilenameParser` 对齐 Kodi / Emby.Naming 规则（`电影.年份.1080p`、`剧集.S01E01`、年份校正、剧集结构判断），而非每请求一次。
- **中文优先**：`searchMulti` 带 `language=zh`；预留豆瓣等中文 Provider 提升中文命中（§9.2 决策点 4）。
- **手动匹配兜底**：冷门/不规范命名的进「其他」分类，用户可手动确认；命中后写回本地缓存，后续同标题秒回。

#### 落地载体（对应新增模块）
- Room 缓存表：`scrape_cache`（`query_key` PK、json、fetched_at、TTL）——由 `:core:library` 的 `MetadataCache` 管理，与现有 `media_item` 解耦（缓存是"识别结果"，`media_item` 是"海报卡片"）。
- 图片缓存目录：复用 `:core:thumbnail` 的缓存路径与 `.thumb/` 上传约定，不另起一套。

---

## 7. 分阶段实施

> 沿用项目约定，以 `[phase-n]` 前缀提交。每阶段保持可编译、可回归。

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| **Phase A** | `:core:network` 新增 `tmdb/*`（`TmdbApi`、DTO、Moshi adapter）；`FilenameParser` 单测 | TMDB 客户端 + 名称清洗模块（可独立测试） |
| **Phase B** | `media_item` + `scrape_cache` 表 + `video.group_key` 列 + `MediaItemDao`/`ScrapeCacheDao` + 领域模型 + `LibraryRepository` | 数据层就绪；`ContentType`/`ScrapeState` 枚举 + Converter |
| **Phase C** | `LibraryGrouper`（本地走 `video.group_key` 索引，远程走 `listFiles` 结果缓存）+ `ScraperEngine`（本地缓存命中/动态限流/指数退避/增量/NFO 优先）+ `MetadataCache` | 分组→刮削闭环 + 识别缓存，单源可端到端跑通 |
| **Phase D** | `:feature:library` 海报墙网格 + 设为源/移除源 + 搜索筛选 + 详情页 + 点海报实时解析播放 | 首版海报墙可用（与文件浏览并行） |
| **Phase E** | 多源并行、剧集季/集详情、`.nfo`/封面写回（Kodi 兼容）、刷新/手动匹配、增量同步、性能调优 | 打磨与兼容增强 |

---

## 8. 风险与降级

| 风险 | 应对 |
|------|------|
| **实时分组性能** | 本地源走 `video.group_key` 索引列；远程源用 `listFiles` 结果的内存态缓存；均不重扫文件系统。超大目录封顶为毫秒级查询 |
| TMDB **限流 / 无 Key** | 设置页开放 TMDB API Key（沿用 `:core:datastore`）；限流 + 退避；Key 缺失降级为「仅文件名 + 本地封面」 |
| 中文匹配不准确 | `searchMulti` 优先 `language=zh`；预留 Douban Provider；提供「校验/手动匹配」页 |
| 大目录扫描耗时 | 扫描/刮削后台执行，UI 订阅进度 StateFlow；按源目录增量扫描 |
| 海报墙与文件浏览数据漂移 | 不建映射表，文件↦条目实时/缓存推导，天然与文件浏览一致；`video.group_key` 仅为派生缓存随扫描重建 |

---

## 9. 决策记录与待定项

### 9.1 已定（本次评审）
- **落地强度：中**（有效表 `media_item`（条目）+ `scrape_cache`（识别缓存）+ `video.group_key` 缓存列；不建 `library_source` / `media_item_file` 映射表）。
- **与文件浏览关系：并行共存**（文件浏览保留，海报墙为新增入口/视图，共享文件数据与播放链路）。

### 9.2 待评审决策点
1. **模块落位**：是否新建 `:core:library` + `:feature:library`（本方案倾向），还是先收敛在 `feature:home`？
2. **远程源分组缓存**：采用「内存态缓存随源刷新」，还是需要一个轻量持久缓存增强超大型远程库体验？（更接近"重"，但可做成可选）
3. **刮削结果写回**：一期是否要 `.nfo` + 封面写回源目录（Kodi 兼容）？还是仅存本机 Room + 缓存目录？
4. **中文数据源**：一期仅 TMDB，豆瓣 Provider 是否列入后续排期？
5. **海报墙位置**：独立 Tab，还是首页内的次级入口（与「最近播放（视频/音频）」并列）？