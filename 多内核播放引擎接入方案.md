# NIplayer 多内核播放引擎接入方案

> 版本：v1.0
> 日期：2026-09-02
> 定位：在既有「media3 单一内核」基础上，预留可插拔的多内核接入能力，并给出 mpv 内核的完整接入路径。
> 前置约束：本方案**不推翻**「以 media3 为默认内核」的架构决策；mpv 作为**可选兜底内核**纳入。

***

## 0. 结论速览

| 问题         | 结论                                                                                                    |
| ---------- | ----------------------------------------------------------------------------------------------------- |
| mpv 是否支持硬解 | **支持**。Android 上通过 `hwdec`（`mediacodec` / `vulkan`）硬解，并内置 FFmpeg 软解兜底。定位是「硬解优先 + 软解 fallback」，而非「纯软解」 |
| 是否立引 mpv   | **否**。存量痛点（长尾硬解不了 / ASS 特效弱 / HDR 色彩）可按场景定点补强；整体换/加内核需先完成「NxPlayer 去 media3 化」的抽象改造，否则双内核维护成本高        |
| 本方案做什么     | ① 把 `NxPlayer` 抽象抽离 media3 专属类型；② 建立内核注册 + 能力驱动的选择机制；③ 给出 MpvBackend 完整接入路径，作为可随时落地的第二内核              |

***

## 1. 现状与问题

### 1.1 现状（以代码为准）

- 播放内核：`androidx.media3` 单一内核（`player/kernel`）。

- 内核抽象：`NxPlayer` 接口，Hilt `@Binds` 仅绑定 `NxMedia3Player`。接口注释明确「无工厂模式，单一 media3 内核」。

- 软解：**仅音频** FFmpeg 扩展（`player:ffmpeg`，14 个音频 decoder）。

- 视频解码：依赖 MediaCodec **硬件解码**，无视频软解扩展。

- 字幕渲染：内嵌走 media3 TextRenderer；外挂 ASS/SRT 走 Compose Canvas 自绘。

- 渲染宿主：视频 = 独立 Activity + SurfaceView；音频 = 导航内。

### 1.2 核心问题：`NxPlayer` 与 media3 强耦合，无法廉价插拔第二内核

当前 `NxMedia3Player` 在实现 `NxPlayer` 时直接 import 大量 media3 类型与语义，导致抽象层混入了 media3 专属细节：

| 泄漏点   | 现状                                                                                          | 影响                         |
| ----- | ------------------------------------------------------------------------------------------- | -------------------------- |
| 字幕类型  | `NxPlayer.cues: StateFlow<List<androidx.media3.common.text.Cue>>`                           | UI 层直接依赖 media3 `Cue` 结构   |
| 轨道语义  | `SubtitleTrackInfo.isAutoSelected` 依赖 media3 语言偏好自动选择                                       | mpv 无同等语义，需翻译              |
| 缩放语义  | `setBlackBarCropEnabled` 映射 media3 `SCALE_TO_FIT_WITH_CROPPING`；`Stretch` 靠 UI 调 Surface 尺寸 | mpv 无对应 `videoScalingMode` |
| 倍速/音调 | `pitchPreservation` / `playbackSpeed` 依赖 media3 `PlaybackParameters`                        | mpv 需命令映射                  |
| 附加依赖  | `equalizer` 绑定 media3 `audioSessionId`；`events` 含 media3 回调语义                               | 每内核一套适配                    |

> 结论：**接入 mpv 的前置工作是把** **`NxPlayer`** **去 media3 化**。否则第二内核需为上述每一项写翻译层，等于维护两套「状态/轨道/同步」协议，违背项目「接口干净、彻底移除遗留」的偏好。

***

## 2. 目标架构：能力驱动的可插拔内核

```
                    ┌─────────────────────────────────────────┐
                    │           UI / PlayerViewModel           │
                    │      (只面向 NxPlayer 抽象，不感知内核)   │
                    └───────────────────┬─────────────────────┘
                                        │
                    ┌───────────────────▼─────────────────────┐
                    │           NxPlayer (去 media3 化接口)    │
                    │   NxCue / NxVideoScale / NxPlaybackParams│
                    └───────────────────┬─────────────────────┘
                                        │ Hilt @IntoSet
                ┌───────────────┬───────┴──────┬───────────────┐
                ▼               ▼              ▼               ▼
         NxPlayerBackend   NxPlayerBackend NxPlayerBackend   ...
         Media3Backend      SoftVideoBackend MpvBackend
         (默认，硬解)     (media3+软解扩展)  (硬解+软解+libass)
                └── 由 NxPlayerProvider 按能力/偏好选择，可联动 ┘
```

### 2.1 新增抽象：`NxPlayerBackend`

```kotlin
interface NxPlayerBackend : NxPlayer {
    /** 唯一标识，如 "media3" / "mpv" / "media3-soft" */
    val backendId: String

    /** 是否可处理该媒体源（格式/容器/字幕类型探测）。 */
    fun supports(source: NxMediaSource): Boolean

    /** 表示优先级/偏好，用于能力解析（数值大优先）。 */
    val preference: Int

    /** 声明该内核是否在同一 media3 引擎上叠加软解等扩展位。 */
    val variantOf: String?   // 如 MpvBackend variantOf = null；SoftVideo variantOf = "media3"
}
```

实现类通过 Hilt：

```kotlin
@Module
@InstallIn(SingletonComponent::class)
abstract class PlayerBackendModule {
    @Binds @IntoSet
    abstract fun media3(b: NxMedia3Player): NxPlayerBackend

    // 未来:
    // @Binds @IntoSet abstract fun mpv(b: NxMpvPlayer): NxPlayerBackend
}
```

### 2.2 新增解析器：`NxPlayerProvider`

```kotlin
interface NxPlayerProvider {
    /** 按 capability 解析出最佳内核；可返回多个形成 fallback 链。 */
    fun resolve(source: NxMediaSource): List<NxPlayerBackend>
}
```

选择逻辑（能力驱动）：

1. 所有 `@IntoSet` 后端中，过滤 `supports(source) == true` 的候选。
2. 按 `preference` 降序排序；同优先级按注册序稳定。
3. 首个为「首选」，其余为「fallback」。`PlayerViewModel` 观察到首选内核 `state == ERRORED`（且 codec 缺失类错误）时，逐级降级到下一个内核，并**跨内核续演**（见 §5）。

### 2.3 首选内核配置

- 全局设置项 `PlayerSettings.playerBackend`（`"auto"` / 指定内核 id）。

- `"auto"` 时走能力解析；否则固定使用指定内核（常用来强制 mpv 软解排障）。

***

## 3. `NxPlayer` 去 media3 化改造（前置工作）

目标：让接口不再依赖 `androidx.media3.*`，仅保留传输无关语义。

### 3.1 新增自有类型（`player/kernel`）

```kotlin
// 传输无关字幕 cue（替代 media3 Cue）
data class NxCue(
    val text: String?,
    val startMs: Long,
    val endMs: Long,
    val positionPct: Float?,       // 0..1，null=默认
    val lineAnchor: Int?,          // 对齐语义，替代 media3 line-anchor
    val textSizePct: Float?,
    val style: NxSubtitleStyle?,   // 字号/颜色/描边等自有结构
)

// 缩放语义（替代 videoScalingMode + Stretch 外包）
sealed interface NxVideoScale {
    object Fit : NxVideoScale
    object Crop : NxVideoScale
    data object Stretch : NxVideoScale
    data object Ratio16_9 : NxVideoScale
    // 智能黑边:=Crop,但保留"有效画面比例"供 UI 决策
    data class BlackBarCrop(val visibleAspect: Float) : NxVideoScale
}

// 倍速/音调（替代 PlaybackParameters）
data class NxPlaybackParams(val speed: Float, val pitch: Float)
```

### 3.2 接口调整对照表

| 现状                                 | 去 media3 后                                                                   | 说明                                                               |
| ---------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `cues: StateFlow<List<Cue>>`       | `cues: StateFlow<List<NxCue>>`                                               | UI 改消费 `NxCue`                                                   |
| `SubtitleTrackInfo.isAutoSelected` | 移除该字段                                                                        | UI 改为读「当前生效字幕的确定性状态」，由各内核自行给出 `selectedSubtitleTrackIndex` 实际命中项 |
| `setBlackBarCropEnabled(Boolean)`  | 并入 `setVideoScale(NxVideoScale)`                                             | 统一到自有枚举，删掉 `videoScalingMode` 细节                                 |
| `playbackSpeed/pitchPreservation`  | 统一为 `params: StateFlow<NxPlaybackParams>` + `setPlaybackParams(speed,pitch)` | 不再暴露 `PlaybackParameters`                                        |
| `equalizer`                        | 保留 `NiEqualizer`，接口改为按 `audioSessionId` 注入方式抽象（每内核自实现 attach）                | 属性本身与内核解耦                                                        |
| `resetStatus` 等内部回调                | 收敛为 `events: SharedFlow<PlaybackEvent>`                                      | ——                                                               |

> 改造注意：去媒体3化是**兼容性重构**，凡涉及删除字段（`isAutoSelected`）需同步修 `NxMedia3Player` 实现与调用方 UI/ViewModel，避免编译破坏。

***

## 4. mpv 内核接入方案（MpvBackend）

### 4.1 构件与依赖

| 项      | 方案                                                           | 备注                                                  |
| ------ | ------------------------------------------------------------ | --------------------------------------------------- |
| libmpv | 自编译 mpv-android 或引入社区维护的 libmpv **AAR**                      | 优先自编译以对齐 NDK 28.2 / CMake 3.22 工具链；复用现有 FFmpeg 编译经验 |
| 架构     | arm64-v8a + armeabi-v7a（对齐 `player:ffmpeg`）                  | 其余 ABI 从架构中裁剪                                       |
| 软解     | mpv 内置 FFmpeg（video 解码器随全量 libavcodec 编译进入）                  | 覆盖 MediaCodec 缺失的 HEVC/AV1/10bit 等长尾                |
| 硬解     | `--hwdec=mediaCodec` / Vulkan `gpu-context`；`auto` 优先硬解失败切软解 | **mpv 支持硬解**，不牺牲性能                                  |
| libass | **使用 mpv 内嵌版本**，不单独引入 libass 依赖                              | 规避项目「弃用 libass」约束下的版本兼容问题；承接复杂 ASS 特效渲染             |

### 4.2 渲染接入

- **视频**：mpv `render API`（GLES）渲染到 `GLSurfaceView` / `TextureView`，或直接挂 `Surface`；与现有「独立 Activity + SurfaceView」宿主衔接。

  - 退出转场、黑边蒙层、fade 动画沿用现有机制（mpv 渲染层作为子层随窗口参与转场）。

- **音频**：**不引入 mpv 音频**。音频内核维持 media3 导航内实现。`MpvBackend` 仅作为**视频内核候选**，避免音频双内核。

### 4.3 状态/事件桥接

将 mpv 的 property observe 与命令映射到去媒体3化后的 `NxPlayer`：

| NxPlayer 语义                                | mpv 映射                                                                    |
| ------------------------------------------ | ------------------------------------------------------------------------- |
| `state: StateFlow<PlaybackState>`          | observe `pause` / `eof-reached` / `core-idle` / `paused-for-cache`        |
| `positionMs` / `durationMs`                | property `time-pos` / `duration`                                          |
| `videoSize`                                | property `video-params` (w/h/aspect) + `video-dec-params`                 |
| `cues`（翻译为 `NxCue`）                        | read `sub-text` / demux track；复杂 ASS 走 `sub-visibility` + libass 渲染在视图层叠加 |
| `mediaInfo`                                | `video-params` / `audio-params`(codec,bps,samplerate) / `video-format`    |
| `setSource/prepare/play/pause/seekTo`      | `loadfile` / `play` / `pause` / `seek`                                    |
| `setPlaybackParams(speed,pitch)`           | `speed` / `af`(scaletempo) 保持音调                                           |
| `selectAudioTrack` / `selectSubtitleTrack` | `aid` / `sid`（-1 自动，-2 关闭）                                                |
| `setSubtitleOffsetMs`                      | `sub-delay`                                                               |
| `setLooping`                               | `loop-file=inf` / `loop-playlist`                                         |

### 4.4 能力声明

```kotlin
class NxMpvPlayer @Inject constructor(
    private val context: Context,
    private val sourceBuilder: MediaSourceBuilder,
) : NxPlayerBackend {

    override val backendId = "mpv"
    override val preference = 90          // 低于 media3 默认，作为兜底
    override val variantOf: String? = null

    override fun supports(source: NxMediaSource): Boolean {
        // 命中以下任一即倾向 mpv:
        //  1) MediaCodec 无法硬解的视频容器/编码（探测失败）
        //  2) 外挂/内嵌包含复杂 ASS 特效（弹幕、渐变、\pos/\fade）
        //  3) HDR / Dolby Vision 片源（色彩映射更优）
        return source.hasHighReqVideoCodecNotHwDecodable()
                || source.hasComplexAssSubtitle()
    }
}
```

### 4.5 跨内核 fallback（续演协议）

Kernel A（media3）→ 播放失败（`codec` 缺失 / `format` 不支持）→ 降级到 Kernel B（mpv）：

1. `PlayerViewModel` 捕获 `events` 中的 `PlaybackEvent.Error`，判定为可降级错误。
2. 冻结当前进度 `positionMs`、`subtitleOffsetMs`、`selectedTracks`、`playbackParams`。
3. 通过 `NxPlayerProvider` 取下一候选内核，重新 `setSource(source, startPositionMs=positionMs)` → `prepare` → `play`。
4. 首帧渲染事件到达后，持久化标记「该源优先用 mpv」到缓存，避免每次重走降级。
5. 失败概率兜底：连续 N 次降级失败则停在此内核并展示错误。

***

## 5. 落地节奏与风险

### 5.1 建议分 3 步，第 1 步即可合入，步骤 2/3 按需触发

**Step 1（建议做）：接口去媒体3化 + 新增** **`NxPlayerBackend`** **/** **`NxPlayerProvider`** **骨架**

- 纯重构，不改默认行为；media3 仍是唯一已注册内核。

- 产出：`NxPlayer` 升级为去媒体3化接口，删除 `isAutoSelected` 等泄漏点；`@IntoSet` 注册机制就绪。

- 验收：现有播放全流程回归通过；编译零警告。

**Step 2（可选）：media3 视频软解扩展（SoftVideoBackend）**

- 给 `player:ffmpeg` 增加视频解码器（libavcodec video），作为 MediaCodec 失败时的软解 fallback，仍在同一 media3 引擎上。

- 产出：`variantOf="media3"` 的软解后端。

- 收益：低成本覆盖「硬解不了」长尾，不引入第二内核进程/渲染。

**Step 3（可选，需明确长尾需求时才做）：MpvBackend**

- 前置条件：Step 1 已合入且稳定。

- 按 §4 落地 libmpv、渲染桥接、命令映射、capability 声明与跨内核续演。

- 成本最高，收益集中在「复杂 ASS 特效 + HDR/DV 色彩」场景。

### 5.2 风险评估

| 风险           | 影响                  | 对策                                                        |
| ------------ | ------------------- | --------------------------------------------------------- |
| 去媒体3化破坏现有 UI | 编译/回归失败             | 同步更新 `NxMedia3Player` 与所有引用方；大改时以 Step 为单位分批合入            |
| 双内核渲染/生命周期混乱 | 视频画面与 UI 撕裂、内存泄漏    | 严格走 `attachSurface`/`release` 生命周期；视频 mpv 在独立 Activity 承载 |
| libass 版本兼容  | mpv 复用内嵌版本不受该项目约束影响 | 不单独引入 libass 依赖                                           |
| APK 体积 / ABI | 增加数 MB\~20MB/ABI    | 仅编 arm64+v7；mpv 可用 `split-per-abi` 分发包                    |
| 跨内核续演状态一致性   | 进度/轨道/字幕偏移错位        | 明确续演协议与冻结字段集合，写单测                                         |

***

## 6. 待办清单（Checklist）

- [ ] Step 1-1：在 `player/kernel` 新增 `NxCue` / `NxVideoScale` / `NxPlaybackParams` 自有类型

- [ ] Step 1-2：改造 `NxPlayer` 接口，删除 media3 专属语义（`cues` 类型、`isAutoSelected`、`videoScalingMode`、`PlaybackParameters`）

- [ ] Step 1-3：同步更新 `NxMedia3Player` 实现

- [ ] Step 1-4：新增 `NxPlayerBackend` 接口 + Hilt `@IntoSet` 注册 + `NxPlayerProvider.resolve`

- [ ] Step 1-5：更新所有消费 `cues`/轨道\`的 UI/ViewModel，回归全流程

- [ ] Step 2（可选）：`player:ffmpeg` 增加视频软解；注册 `SoftVideoBackend`

- [ ] Step 3（可选）：自编译 libmpv；接入渲染；§4 桥接表实现；注册 `MpvBackend`；实现跨内核续演与优先级缓存

