<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xyoye.local_component">

    <application
        android:name="com.xyoye.common_component.base.app.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="q">
        <activity android:name=".ui.activities.main.MainActivity"
            android:exported="true" />

        <activity android:name=".ui.activities.play_history.PlayHistoryActivity" />
        <activity android:name=".ui.activities.quick_access.QuickAccessActivity" />
    </application>

</manifest>