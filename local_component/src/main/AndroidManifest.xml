<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".ui.activities.play_history.PlayHistoryActivity"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".ui.activities.quick_access.QuickAccessActivity"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".ui.activities.shooter_subtitle.ShooterSubtitleActivity"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".ui.activities.bind_source.BindExtraSourceActivity"
            android:theme="@style/ShareElementActivityTheme"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".ui.activities.scrape.ScrapeMediaActivity"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".ui.activities.scrape.MuluSettingActivity"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".ui.activities.scrape.ScrapeDetailActivity"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".ui.activities.scrape.SearchMatchActivity"
            android:configChanges="orientation|screenSize" />
    </application>

</manifest>