# 歌单系统重设计方案

> 版本：v1.1　|　状态：方案评审稿（仅设计，不改代码）　|　适用范围：局域网存储媒体播放器「歌单」能力
> 变更：v1.1（2026-08-27）解除「不引入新业务功能」硬约束，新增歌单 **UI/UX 重设计**章节（§10）；v1.0 为分层/数据模型/流程重设计。

本报告针对当前歌单系统做**逻辑层与交互层**的整体重设计，分为两大部分：
- **逻辑侧（§1–§9）**：技术债盘点、分层、数据模型、核心流程的重设计；全程 Kotlin + Coroutines + StateFlow，在不改变技术栈的前提下按 clean architecture 落地。
- **体验侧（§10）**：歌单功能 UI/UX 重设计——允许在既有架构上**新增与重设计业务功能**，补齐当前交互缺口、统一视觉语言与操作范式。

---

## 1. 背景与目标

### 1.1 现状一句话
歌单能力由「`playlist` + `playlist_item` 两张 Room 表」承载，业务逻辑散落在 `PlaylistItemDao` 的事务方法、两个 ViewModel、以及 `PlayStarter` / 备份实现里，缺少统一的领域层与数据一致性保障。

### 1.2 重设计目标
| 目标 | 说明 |
|------|------|
| 分层清晰 | 把业务事务逻辑从 Dao 抽出到领域层，Dao 回归纯 SQL |
| 单一职责 | ViewModel 不再兼任封面生成 / 存储源创建等多职责 |
| 数据一致性 | 用 Room 外键 + 级联解决「悬空引用」与「删除不同步」 |
| 可测试性 | 引入 Repository 边界，业务逻辑可脱离 DB/Storage 单测 |
| 命名归口 | 领域对象、DB Entity、player kernel 之间映射集中，消除散落 DTO |

---

## 2. 现状盘点

### 2.1 数据模型
- [PlaylistEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/PlaylistEntity.kt) —— `playlist` 表：`id / name / created_at / updated_at / is_pinned`
- [PlaylistItemEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/PlaylistItemEntity.kt) —— `playlist_item` 表：`id / playlist_id / library_id / file_path / file_name / media_type / file_size / sort_order`，唯一索引 `(playlist_id, file_path)`

### 2.2 数据访问层（Dao）
- [PlaylistDao.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/dao/PlaylistDao.kt) —— 歌单 CRUD + 排序/置顶查询
- [PlaylistItemDao.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/dao/PlaylistItemDao.kt) —— 条目查询/增删，并**内置了 6 个事务业务方法**：`duplicatePlaylist` / `mergeInto` / `copyItemsTo` / `moveItemsTo` / `addItems` / `persistOrder`

### 2.3 业务/界面层
- [PlaylistsViewModel.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistsViewModel.kt) —— 歌单列表页；同时负责封面生成
- [PlaylistDetailViewModel.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistDetailViewModel.kt) —— 详情页；同时负责封面生成、播放启动、跨歌单复制/移动/合并
- [PlaylistsScreen.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistsScreen.kt) / [PlaylistDetailScreen.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistDetailScreen.kt) —— Compose UI
- [PlaylistMappers.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistMappers.kt) —— `PlaylistItemEntity ↔ player.kernel.PlaylistItem` 映射

### 2.4 跨切面集成
- [PlayStarter.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/PlayStarter.kt) —— 承载**来源歌单 / 播放历史 / 快速访问**三条启动链，其中 `restorePlaylistFromDb` / `startFromPlaylist` 属歌单逻辑
- [PlaylistSaveViewModel.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/player/src/main/java/com/nichx/niplayer/feature/player/PlaylistSaveViewModel.kt) —— 播放页「添加到歌单」
- [PlaylistHolder.kt](file:///Users/nichx/Documents/GitHub/NIplayer/player/kernel/src/main/java/com/nichx/niplayer/player/kernel/PlaylistHolder.kt) —— 全局可变单例，承载「同目录连播」与「歌单播放」两种语义
- [PlaylistBackupItem.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/backup/table/PlaylistBackupItem.kt) —— 备份/恢复（含 `playlist_id` 重映射）

---

## 3. 问题清单

按严重度分级：

### 🔴 P0 数据一致性
1. **悬空引用无兜底**：`playlist_item.library_id` 指向的媒体库被删除、或 `file_path` 对应的远程文件缺失时，歌单条目仍是「死条目」，点击播放直接失败。当前无级联、无清理、无过期标记，用户无法在 UI 感知失效条目。
2. **端到端删除不同步**：删除媒体库、清理文件缓存等操作与 `playlist_item` 无联动，需手动 `deleteByPlaylist`，一旦漏调即产生孤儿条目。

### 🟠 P1 分层与职责
3. **Dao 里塞业务事务**：`duplicatePlaylist/mergeInto/copyItemsTo/moveItemsTo/addItems/persistOrder` 是纯业务规则（去重、重映射 id、排序分配），却写成 Room Dao 扩展函数。导致：业务无法脱离 DB 测试、Dao 与业务强耦合。
4. **ViewModel 职责过载**：两个歌单 ViewModel 各自复制了一份 `resolveCover`（依赖 `MediaLibraryDao/StorageFactory/ThumbnailManager` 造缓存封面），与「列表展示」正交；`PlaylistDetailViewModel` 同时管播放启动 + 跨歌单操作，职责混杂。
5. **`PlayStarter` 上帝类**：一条类内聚合「历史/快速访问/歌单」三条启动逻辑，歌单相关路径与杂项混排，难以演进。

### 🟡 P2 数据模型缺陷
6. **唯一索引不含 `library_id`**：`(playlist_id, file_path)` 无法区分「不同存储源的同路径文件」，异库同路径会被错误去重/覆盖。
7. **`sort_order` 分配用 COUNT 而非 MAX**：`addItems` 以 `countByPlaylist` 做下一序号，删除中间项/重复跳过会导致序号冲突或断档；并发追加时计数不一致，拖拽 `persistOrder` 只改变化行，可能留下重复序号。
8. **媒体类型约束不明**：歌单名语义为「歌单」，播放路径只过滤音频，但表结构允存视频/图片，字段含义与行为不统一。

### 🟢 P3 可维护性
9. **DTO 映射散落**：`PlaylistItemEntity`(DB) / `PlaylistItem`(kernel) / 备份 JSON / 领域对象 四处形态，映射仅覆盖字段透传，无单一所有权。
10. **PlaylistHolder 全局可变单例**：`@Volatile set/consume/clear` 手动同步 + 事件流，多生产者竞争、跨会话残留风险，且单例与 ViewModel 生命周期无契约。
11. **备份 id 重映射手写**：若 `playlist` 表新增字段，`PlaylistBackupItem` 需同步演进，无版本化结构。

---

## 4. 目标架构

### 4.1 分层模型
```
UI  (feature:home / feature:player)
     │  StateFlow 订阅 / 触发意图
     ▼
用例层  PlaylistUseCase (创建/删除/复制/合并/移动/排序/添加到歌单/组装播放项)
     │
     ▼
领域层  PlaylistRepository (接口)
     │  事务性业务规则，返回领域对象，不暴露 DB/Storage
     ▼
数据层  RoomPlaylistRepository (实现 :core:database)
          └─ PlaylistDao / PlaylistItemDao（纯 SQL）
```
- **Dao 回归纯 SQL**：移除 6 个事务业务方法，改为纯增删改查 + 基础事务原语（供 Repository 编排）。
- **Repository 承载规则**：唯一索引去重、id 重映射、`sort_order` 分配、级联删除、失效条目清理。
- **View 只消费 StateFlow 与事件**：不再直接依赖 Dao、StorageFactory、ThumbnailManager。

### 4.2 新增/调整模块归属
| 组件 | 建议位置 | 说明 |
|------|----------|------|
| `Playlist`/`PlaylistItem` 领域模型 | 新增 `:core:playlist`（或先置于 `feature:home` 的领域包） | 与 DB Entity、kernel 解耦 |
| `PlaylistRepository` 接口 | `:core:playlist` | 供 home/player 两个 feature 共用 |
| `RoomPlaylistRepository` 实现 | `:core:playlist`（依赖 `:core:database`） | Hilt 绑定 |
| 封面解析 `PlaylistCoverProvider` | `:feature:home` 或 `:core:thumbnail` | 抽离两处重复 `resolveCover` |
| `PlaylistUseCase` | `:feature:home` | 编排 Repository + 播放启动 |

> 说明：是否新建 `:core:playlist` 与 `:core:thumbnail` 由评审拍板；最小改动是**先在 `feature:home` 内把 Repository 接口与实现 / UseCase / 封面 Provider 划分清楚**，再视复用度提层。

---

## 5. 数据模型重设计

### 5.1 playlist_item（关键列调整）
```kotlin
@Entity(
    tableName = "playlist_item",
    // ① 唯一性纳入 library_id，区分跨存储同路径
    indices = [Index(value = ["playlist_id", "library_id", "file_path"], unique = true)],
    // ② 外键级联：删歌单自动删条目；该库被删时自动清掉相关条目（消除悬空引用）
    foreignKeys = [
        ForeignKey(
            entity = PlaylistEntity::class,
            parentColumns = ["id"],
            childColumns = ["playlist_id"],
            onDelete = ForeignKey.CASCADE,
        ),
        ForeignKey(
            entity = MediaLibraryEntity::class,
            parentColumns = ["id"],
            childColumns = ["library_id"],
            onDelete = ForeignKey.CASCADE,
        ),
    ],
)
data class PlaylistItemEntity(
    @PrimaryKey(autoGenerate = true) var id: Int = 0,
    @ColumnInfo(name = "playlist_id") var playlistId: Int,
    @ColumnInfo(name = "library_id") var libraryId: Int,
    @ColumnInfo(name = "file_path") var filePath: String,
    @ColumnInfo(name = "file_name") var fileName: String,
    @ColumnInfo(name = "media_type") var mediaTypeValue: String, // 收敛为纯音频语义，见 §5.3
    @ColumnInfo(name = "file_size") var fileSize: Long = 0L,
    @ColumnInfo(name = "sort_order") var sortOrder: Int = 0,
)
```
**收益**：
- 删除歌单 = 一条 `DELETE FROM playlist`，条目由 CASCADE 自动清理，省去手工 `deleteByPlaylist`。
- 删除媒体库 = 相关歌单条目自动清理，杜绝孤儿引用。

### 5.2 sort_order 分配规则
- `addItems` 起始序号改为 `COALESCE(MAX(sort_order), -1) + 1`（而非 COUNT），避免断档/冲突。
- 新增 `reflowSortOrder(playlistId)`：整批重写 `sort_order = 0..n`，删除/移动/合并后调用，保证序号连续。

### 5.3 媒体语义收敛
- `playlist_item` 仅收录**音频**：所有入口（保存到歌单 / 添加到歌单）在边界处过滤 `isAudioFile`。
- `media_type` 仍保留 `mediaTypeValue`（与 `player.kernel.PlaylistItem` 字段对齐，用于构造 HistoryDescriptor），但不在 ROW 级承载视频。
- 若个别场景确需视频条目（如未来「追剧歌单」），作为后续独立需求再扩展，本次不引入。

### 5.4 失效条目策略（边界内最小化）
方案二选一，评审确定：
- **A（推荐，不新增功能）**：保留失效条目，启动播放前按「库已删除 → 跳过；文件不存在 → 跳过并给出标题级提示」，维持现状行为但统一收纳到 Repository 的 `resolvePlayableItems`。
- **B（超出本报告范围）**：引入 `is_resolved` 标记与「失效条目去灰/删除」交互——属新业务功能，明确不做，仅记录备选。

---

## 6. 核心流程重设计

### 6.1 新增 / 重命名 / 置顶（归属 PlaylistUseCase → Repository）
```
create(name)      → trim 校验 → repo.createPlaylist(name)
rename(…) / pin(…) → repo.renamePlaylist / repo.pinPlaylist
```
- ViewModel 只保留 `trim + 空名拦截` 的 UI 校验，落库与 `updated_at` 维护统一进 Repository。

### 6.2 添加到歌单（history / player「保存到歌单」共用）
```
addToPlaylist(playlistId, items, newName?):
    audioItems = items.filter { isAudioFile }        // 边界过滤
    playlistId = target == New ? repo.createPlaylist(newName) : target
    repo.appendItems(playlistId, audioItems)          // 内部: MAX(sort_order)+1 + 唯一索引去重
    repo.touch(playlistId)                            // 更新 updated_at
```
替换 `PlaylistItemDao.addItems` 与 `PlaylistSaveViewModel` 内嵌逻辑；新/已有歌单两条路径收敛到同一 UseCase。

### 6.3 跨歌单复制 / 移动 / 合并
```
copyItemsTo(targetId, itemIds) → repo.copyItems(targetId, ids)
moveItemsTo(targetId, ids, srcId) → repo.moveItems(targetId, ids, srcId)   // 事务：插入+删源+双 touch+reflow
mergeInto(srcId, targetId) → repo.mergePlaylist(srcId, targetId)           // 事务：追加+reflow
duplicate(srcId, newName) → repo.duplicatePlaylist(srcId, newName)         // 事务：建歌单+拷贝条目
```
- 事务全部下沉到 `RoomPlaylistRepository`（`db.withTransaction`），`PlaylistDetailViewModel` 只负责编排与提示文案。
- 所有「插入后」操作在事务尾部调用 `reflowSortOrder(targetId)` 保证序号连续。

### 6.4 拖拽排序
```
onDragEnd → repo.replaceOrder(playlistId, orderedIds)   // 事务：整表重写 sort_order 0..n + touch
```
替代逐行 `update(copy(sortOrder))`，原子、无重复序号。

### 6.5 播放
- **播放全部 / 播放到某曲（`Repository.resolvePlayableItems(playlistId, startFilePath)`）**：
  - 读条目 → 过滤音频 → 组 `List<PlaylistItem>` → 计算 startIndex。
  - 供 `PlayStarter` 与详情页共用，替换 `restorePlaylistFromDb` / `playAll` / `playItem` 内的重复组装。
- `PlayStarter` 拆分为：
  - `HistoryStartProvider`（历史/快速访问）
  - `PlaylistStartProvider`（歌单 → 走 Repository）
  - 由统一入口按场景分发，三个 ViewModel 各取所需。
- 说明：`PlaylistHolder` 改动属 `player:kernel` 播出侧课题，本次仅记录不建议在歌单层继续散落组装逻辑，不展开。

### 6.6 删除媒体库 / 断连自我修复
- 依赖 §5.1 的 `onDelete = CASCADE`：一次删除媒体库事务自动清掉相关歌单条目，无需调用方联动。
- 数据库迁移兜底仍走 `fallbackToDestructiveMigration`（项目既有约束）。

### 6.7 备份 / 恢复
- `PlaylistBackupItem` 保留现有快照结构 `{playlists, items}` 与 `playlist_id` 重映射（逻辑正确），但改为复用 Repository 的 `insertPlaylistWithItems` 作为单事务入口，避免手写 SQL 散落。
- 导出顺序 `playlist → item`、恢复先建歌单再重映射条目两条规则显式化到 Repository 方法注释中（CRM 约束：MERGE 默认 / REPLACE 清空重插）。

---

## 7. 实施步骤（建议分 3 步）

| 阶段 | 内容 | 验收 |
|------|------|------|
| **Step 1 抽领域层** | 新增 `:core:playlist`（或 home 内领域包）：`Playlist`/`PlaylistItem` 领域模型、`PlaylistRepository` 接口、`RoomPlaylistRepository` 实现（含全部事务方法）、Mapper；移除 Dao 的 6 个业务方法 | 编译通过；既有功能无回归；Repository 可脱离 DB 单测 |
| **Step 2 数据模型与一致性** | 调整 `playlist_item`：唯一索引纳入 `library_id`、加入 CASCADE 外键、`sort_order` 改 MAX+reflow | DB schema 迁移通过；删除媒体库/歌单自动清理生效 |
| **Step 3 收敛职责** | 抽 `PlaylistCoverProvider` 合并两处 `resolveCover`；拆分 `PlayStarter`；ViewModel 改为只编排 UseCase；`PlaylistSaveViewModel` 复用 `addToPlaylist` | UI 行为一致；无重复逻辑；代码评审清晰 |

> 每步独立可交付、可回滚；Step 1 不依赖 Step 2（Repository 先基于现状 schema 抽取），Step 3 依赖前两步。

---

## 8. 不做 / 边界
- ✅ **允许新增 / 重设计业务功能**（本版已含 §10 歌单 UI/UX 新增交互）；「不引入新业务功能」约束已解除。未纳入 §10 的仍属后续独立需求（如失效条目交互、视频歌单、云歌单分享）。
- ❌ 本版不改造 `player:kernel` 的 `PlaylistHolder`（播出侧另行立项）。
- ❌ 不引入新依赖；通讯层保持 Room + Hilt + Coroutines + StateFlow（新增交互只复用现有组件，不引第三方 UI 库）。
- ⚠️ DB schema 变更依赖 `fallbackToDestructiveMigration`：**老用户升级会清空本地歌单数据**，需评审确认是否接受（与项目「不考虑老用户兼容」约束一致，见 memory）。

---

## 9. 影响面与风险

| 项 | 影响 / 风险 | 缓解 |
|----|-------------|------|
| Dao 方法移除 | 改接口 = 波及 `PlaylistsViewModel` / `PlaylistDetailViewModel` / `PlaylistSaveViewModel` / `PlayStarter` | Step 1 内同步切换调用方，一次提交 |
| 唯一索引 + 外键变更 | `:core:database` schema 升级 | 由 destructive migration 兜底；Step 2 独立验证 |
| 删除级联语义变化 | 行为从「手动删条目」变为「CASCADE 自动删」 | 以单元测试锁定：删库后无孤儿、删歌单后条目清零 |
| Repository 引入 | 新增一层 = 增加模板代码 | 控制在单一实现类，不为了抽象而抽象 |

---

## 10. 歌单 UI/UX 重设计

> 本节为**体验侧新增/重设计**，与 §1–§9 的逻辑改造松耦合（Logic 可先行，UI 随后接入）。所有视觉沿用 `:core:designsystem`（Material 3 + NiExtraColors + NiTopBar/NiDialog/NiSnackbar 等），不新增第三方 UI 依赖。

### 10.1 现状体验问题（基于现有两个 Screen 的交互复盘）

| 面 | 现状 | 问题点 |
|----|------|--------|
| 列表页 [PlaylistsScreen.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistsScreen.kt) | 单一网格卡片；封面=首条目音频封面或 `QueueMusic` 图标；操作全部藏在长按管理弹层 | ① 置顶只显示角标、无分组，视觉不突出；② 无法在列表页搜索歌单；③ 单条操作需长按→弹层，流程深；④ 无快捷播放入口；⑤ 封面表现单一 |
| 详情页 [PlaylistDetailScreen.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistDetailScreen.kt) | 顶部「播放全部」按钮 + 可拖拽列表；长按进编辑模式、底部批量栏 | ① 无「随机播放」；② 无歌单统计信息（总时长/曲目数）；③ 拖拽排序入口（长按）与多选长按冲突，需靠编辑模式开关；④ 单曲操作隐藏过深；⑤ 无「跳转曲目位置」/分页意识（大歌单全量渲染） |
| 添加入口 [SaveToPlaylistDialog.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/player/src/main/java/com/nichx/niplayer/feature/player/SaveToPlaylistDialog.kt) | 仅播放页一个入口；RadioButton 二选一（新建/已有） | ① 入口单一（文件浏览/历史/最近无法直达）；② 无「已添加」状态反馈 |

### 10.2 信息架构：入口与导航
- 歌单入口收敛为：**首页 Tab「歌单」**（替代当前深处路由）+ 原有 `playlists` 列表路由保留为二级页面，二者共用同一 `PlaylistsScreen`。
- 添加入口统一：文件浏览上下文菜单、播放页、播放历史/最近播放，均调用同一 `AddToPlaylistSheet`（见 §10.6）。
- 详情页跳转参数不变（`playlistId`），导航路由 `Routes.Playlist` 保持兼容。

### 10.3 列表页（Playlists）重设计
- **置顶分组**：列表顶部固定「置顶」区（横向或独立网格），下方「全部」区；置顶区空则不显示该区块，替代当前仅角标。
- **视图切换**：网格（默认）/ 列表 双模式，状态记忆到 `FileBrowserSettings` 类似机制（不新增本地库）。
- **封面升级**：
  - 有封面：4:3 大图（当前 16:9），可叠加「单曲封面 + 渐变底」；有 ≥3 个音频时用三宫格拼贴更贴近音乐类产品。
  - 无封面：统一品牌占位渐变 + 音符图标，替代单一 `QueueMusic`。
- **卡片快捷播放**：卡片右下角悬浮圆形播放按钮（仅当曲目非空），点击直达播放；与点击卡片进入详情分离，避免误触。
- **列表内搜索**：顶栏搜索图标 → 按歌单名过滤（复用详情页 `SearchTopBar` 范式）。
- **创建歌单**：空态用大按钮；非空态用顶栏 `+`（现状）并可在底部抽屉入口保留；空名/全空格内联校验。

### 10.4 详情页（PlaylistDetail）重设计
- **头部信息区**：
  - 大封面 + 歌单名 + 统计行：`XXX 首 · 共 XX:XX · XX MB`（统计由 Repository 聚合，见 §10.7）。
  - 主操作组：**播放全部**（主按钮）+ **随机播放**（次按钮）。
- **列表行交互**：
  - 点击 → 播放该曲并连播；**左滑 → 移除**（`SwipeToDismiss`，系统自带语义，替代必须进编辑态才能删）。
  - 长按多选保留，但**批量操作栏**并入「移动/复制/移除/全选」；拖拽排序独立为**显式拖柄**（当前靠长按，易与多选冲突）。
- **随机播放**：播放顺序 shuffle 化——在 `resolvePlayableItems` 后按随机起始位置处理，逻辑层提供 `startFromPlaylist(playlistId, order=SHUFFLE|SEQUENTIAL)`（见 §10.7 数据映射）。
- **就地编辑排序**：编辑态进入后显示拖柄、条目行高亮，底部「完成」沿用现状；排序落盘仍复用 `persistOrder` 的 Repository 化版本。
- **大歌单**：`LazyColumn` 已有；增加滚动到当前播放曲目的「回到底部播放项」悬浮按钮（跟踪当前曲目 `filePath`），避免手动滚动长列表。

### 10.5 空态 / 加载 / 错误态（统一）
| 场景 | 表现 |
|------|------|
| 无歌单 | 大图标 + 文案 + 「新建歌单」主按钮（沿用 `NiEmptyState`） |
| 详情页无条目 | 「歌单为空」+「去添加」入口触发 `AddToPlaylistSheet` |
| 搜索无结果 | 「无匹配结果」+ 清空搜索（沿用 `NiEmptyState`） |
| 加载中 | 列表页/详情页均已有 Skeleton，沿用 |
| 播放失败 / 条目失效 | 经 `NiSnackbar` 提示，并提示可移除该条目（逻辑层返回失效原因，见 §5.4） |

### 10.6 新增交互组件：AddToPlaylistSheet（共用添加面板）
替代 `SaveToPlaylistDialog` 的单点二选一，改为**一个底部抽屉**：
- 顶部：当前曲目名（或「N 首选中」）。
- **新建分区**：可折叠输入框（新建并添加）。
- **已有歌单列表**：每行显示歌单名 + 曲目数，点击行即添加并给出反馈（行尾短暂 ✓ / Snackbar「已添加到『xxx』」）。
- **已存在态**：该曲已在该歌单时，行内禁用并标「已在歌单」。
- 入口：播放页（现状）、文件浏览长按多选、播放历史/最近播放点击项、详情页空态「去添加」。

### 10.7 体验 → 逻辑 数据映射（供 §6 扩展）
| 新增交互 | 需要的逻辑/数据能力 | 落地位置 |
|----------|--------------------|----------|
| 随机播放 | `startFromPlaylist(playlistId, order=SHUFFLE)` + shuffle 起始索引 | `PlaylistStartProvider` / `Repository.resolvePlayableItems` |
| 歌单统计（总时长/体积） | 汇总 `fileSize` +（可选）音频时长元数据聚合 | `PlaylistRepository` 新查询 |
| 三宫格封面 | 取前 3 个音频条目的封面 | 复用 §6.3 封面 Provider |
| 左滑移除 / 回到底部 | 已是既有 `deleteById` / 现有关键字匹配能力，仅 UI 接线 | View |
| 已存在标记 | 复用唯一索引去重结果（`addedCount==0`） | `addToPlaylist` |
| 置顶分组 | 复用 `is_pinned` 排序查询，仅前端分组渲染 | View |

### 10.8 可访问性与一致性
- 所有图标按钮带 `contentDescription`（沿用现状）。
- 点击目标 ≥ 48dp；左滑移除需二次确认（复用 `NiConfirmDialog`），防误删。
- 视觉延续 `:core:designsystem` 主题：背景分层 `surfaceLevel2/3`、主操作 `primary`、危险操作 `error`、Snackbar 用系统反馈而非 Toast。

### 10.9 UI/UX 落地阶段建议
| 批 | 内容 | 依赖 |
|----|------|------|
| UI-1 | 列表页分组 + 搜索 + 视图切换 + 封面升级 | 无（纯前端） |
| UI-2 | 详情页头部统计 + 随机播放 + 左滑移除 + 回到底部 | 需 §10.7 的 `SHUFFLE` / 统计查询（逻辑 Step 1/2） |
| UI-3 | 统一 `AddToPlaylistSheet` 并接线四处入口 | 需 `addToPlaylist` 用例（逻辑 Step 1） |

> UI 各批可与 Step 1–3 并行推进；不改数据库 schema 的 UI 批（UI-1）可最先落地。

---

## 附：文件速查

| 文件 | 当前问题 | 处理 |
|------|----------|------|
| [PlaylistItemDao.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/dao/PlaylistItemDao.kt) | 业务事务混入 Dao | Step 1 抽出事务，Step 2 加 CASCADE/索引 |
| [PlaylistsViewModel.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistsViewModel.kt) / [PlaylistDetailViewModel.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistDetailViewModel.kt) | 职责过载 | Step 3 抽封面/播放，仅留编排 |
| [PlayStarter.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/PlayStarter.kt) | 三条启动链混排 | Step 3 拆分 |
| [PlaylistSaveViewModel.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/player/src/main/java/com/nichx/niplayer/feature/player/PlaylistSaveViewModel.kt) | 内嵌加歌单逻辑 | Step 3 复用 `addToPlaylist` |
| [PlaylistBackupItem.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/backup/table/PlaylistBackupItem.kt) | 手写 SQL 重映射 | Step 1 复用 Repository 事务入口 |
| [PlaylistEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/PlaylistEntity.kt) / [PlaylistItemEntity.kt](file:///Users/nichx/Documents/GitHub/NIplayer/core/database/src/main/java/com/nichx/niplayer/database/entity/PlaylistItemEntity.kt) | 见 §5 | Step 2 |
| [PlaylistsScreen.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistsScreen.kt) | 置顶/搜索/快捷播放等缺口 | §10.3（UI-1） |
| [PlaylistDetailScreen.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/home/src/main/java/com/nichx/niplayer/feature/home/playlist/PlaylistDetailScreen.kt) | 缺统计/随机/左滑等 | §10.4（UI-2） |
| [SaveToPlaylistDialog.kt](file:///Users/nichx/Documents/GitHub/NIplayer/feature/player/src/main/java/com/nichx/niplayer/feature/player/SaveToPlaylistDialog.kt) | 单入口二选一、无反馈 | §10.6 改为 `AddToPlaylistSheet`（UI-3） |